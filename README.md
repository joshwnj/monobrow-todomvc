# monobrow-todomvc

Example in `src/` borrowed from https://github.com/reactjs/redux/tree/master/examples/todomvc

Give it a burl:

- clone the repo
- `npm install`
- `npm run build`
- `open index.html`
